import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gracefulplatypusclimb',
  appName: 'graceful-platypus-climb',
  webDir: 'dist'
};

export default config;
